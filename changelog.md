`!dq` now supports multiple users in the form `!dq @user @user @user`  
Disabled `!cycle` and `!next` and Cycle Rooms because they are not done
Fixed `undefined` queue members when loading queues from disk